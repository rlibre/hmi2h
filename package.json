{
  "name": "nextion",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
	"build": "esbuild --outfile=bin/main.js --sourcemap --watch --format=iife --bundle --platform=node src/main.ts"
  },
  "dependencies": {
    "@types/node": "^20.10.3"
  }
}
