{
  "name": "nextion",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild --outfile=bin/hmi2h.js --sourcemap --watch --format=iife --bundle --minify --platform=node src/main.ts"
  },
  "dependencies": {
    "@types/node": "^20.10.3",
    "commander": "^11.1.0"
  }
}
